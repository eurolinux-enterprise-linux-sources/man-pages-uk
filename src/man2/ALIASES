exit	_exit

chdir	fchdir

chmod	fchmod

chown	fchown

chown	lchown

dup	dup2

getgid	getegid

getsockopt	setsockopt

getuid	geteuid

llseek	_llseek

open	creat

recv	recvfrom

recv	recvmsg

select	pselect

select	_newselect

send	sendto

send	sendmsg

setregid	setegid

setreuid	seteuid

stat	fstat

stat	lstat

truncate	ftruncate

utime	utimes

wait	waitpid

# [[Category:Системні виклики]]
# [[Category:Сторінки підручника]]